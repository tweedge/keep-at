# Planning/voice documents used during development, not part of the project.
PLAN.md
VOICE.md
VOICE_FINDER_LOG.md

# Build artifacts
/dist/
/keep-at
*.tar.gz

# Go
*.test
*.out

# Local runtime state if keep-at is ever run from the repo root during dev
/keep-at.yaml
